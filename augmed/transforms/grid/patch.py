# Extracts a regular grid of patches from the image.
# Expands these patches along the batch dimension.
# - which may already exist?
# Does this also allow for random patch sampling?
class Patch(GridTransform):
    def __init__() -> None:
        pass


